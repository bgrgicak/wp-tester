{
  "name": "@wp-tester/test-fixtures",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "postinstall": "cd fixtures/wp-tester-plugin && composer install && cd ../wp-tester-theme && composer install",
    "type-check": "tsc --noEmit"
  },
  "description": "Minimal WordPress theme and plugin fixtures for testing",
  "keywords": [
    "wordpress",
    "testing",
    "fixtures"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsc-alias": "^1.8.16",
    "typescript": "^5.9.3"
  }
}
